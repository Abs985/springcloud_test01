server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

#ribbon:
#  ReadTimeout: 5000 # 从服务器读取可用资源需要花费的时间
#  ConnectTimeout: 5000  # 建立连接需要的时间，两端连接需要的时间
feign:
  hystrix:
#    开启hystrix
    enabled: true
spring:
  application:
    name: hystrix-order80

